 #add each topic folder to fpath so that they can add functions and completion scripts
for topic_folder ($DOTFILES/*) if [ -d $topic_folder/functions ]; then fpath=($topic_folder/functions $fpath); fi;