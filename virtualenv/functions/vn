# Set up terminal proxy
proxyon(){
    export ALL_PROXY=$MPROXY
}

proxyoff(){
    unset ALL_PROXY
}
proxystatus(){
    if [[ -n $ALL_PROXY ]];then
        echo -e "Proxy: on"
    else
        echo "Proxy: off"
    fi
}