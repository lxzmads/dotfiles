# Set up terminal proxy
# Virtual Network Environment.
proxyon(){
    export ALL_PROXY=$MPROXY
}

proxyoff(){
    unset ALL_PROXY
}
proxystatus(){
    if [[ -n $ALL_PROXY ]];then
        echo -e "on"
    else
        echo "off"
    fi
}